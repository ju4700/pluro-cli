{
  "name": "pluro-cli",
  "version": "0.2.2",
  "description": "Local-first shared context CLI for agentic tools",
  "main": "dist/index.js",
  "bin": {
    "pluro": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli/index.ts",
    "start": "node dist/cli/index.js",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test \"dist/tests/*.test.js\"",
    "verify:mcp": "npm run build && node scripts/mcp-conformance-check.cjs",
    "verify:ci": "npm run typecheck && npm test && npm run verify:mcp",
    "verify:release-tag": "node scripts/verify-release-tag.cjs",
    "release:check": "npm run verify:ci && npm run verify:release-tag"
  },
  "keywords": [
    "cli",
    "context",
    "agent",
    "mcp"
  ],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "better-sqlite3": "^12.8.0",
    "commander": "^14.0.3",
    "keytar": "^7.9.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/keytar": "^4.4.0",
    "@types/node": "^25.5.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
